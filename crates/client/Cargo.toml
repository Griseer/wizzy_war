[package]
name = "client"
version = "0.1.0"
edition = "2024"

[dependencies]
bevy = "0.18"
shared = { path = "../shared" }
game_core = { path = "../game_core" }
fastrand = "2.0"
net = {path = "../net"}
