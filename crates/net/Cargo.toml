[package]
name = "net"
version = "0.1.0"
edition = "2024"

[dependencies]
bincode = "1.3"
serde = { version = "1.0", features = ["derive"] }
game_core = { path = "../game_core" }
shared = { path = "../shared" }
bitflags = "2.10.0"
